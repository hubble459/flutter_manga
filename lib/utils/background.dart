import 'package:flutter/material.dart';

myBackground() {
  return const BoxDecoration(
      gradient: LinearGradient(
          colors: [(Color(0xFF37373f)), Color(0xFF3a3a3a)], begin: Alignment.topLeft, end: Alignment.bottomCenter));
}
